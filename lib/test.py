#!/usr/bin/env python3
import real_file_collector
import config
import real_file_collector
import payload_parser

for path in real_file_collector.collect(config.RAW_DIR):
	payload_parser.



# data = self.parser.parse(stream)
